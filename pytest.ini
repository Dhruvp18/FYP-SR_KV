[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    gpu: requires a CUDA device (skipped on CPU-only machines)
    slow: real-model runs, minutes not seconds
filterwarnings =
    ignore::DeprecationWarning
